import React from 'react'
import './PlaceOrder.css'

/**
 * Renders a PlaceOrder component.
 * @returns {JSX.Element} A div element containing the text "PlaceOrder".
 */
const PlaceOrder = () => {
  return (
    <div>PlaceOrder</div>
  )
}

export default PlaceOrder